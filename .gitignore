node_modules/
# renderer/ and web/ are 100% generated by scripts/build.js from shared/ — not source.
renderer/
web/
dist/
*.log
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log
# Local offline cache — regenerable from Firestore, contains real student names/attendance (PII).
src/cache.json
# Exported by the "Export Student IDs & PINs" button — real login credentials (PII).
student-ids-pins-*.txt
